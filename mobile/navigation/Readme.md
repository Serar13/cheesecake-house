# Navigation Directory

This directory contains the React Navigation routing setup for the React Native mobile application:

- `AppNavigator.js` - Root navigator coordinating authentication flow (optional) and main tabs.
- `TabNavigator.js` - Bottom tab bar setup linking Home, Cart, and Locations.
- `HomeStackNavigator.js` - Stack navigator supporting transitioning from Menu list to Product Details.
